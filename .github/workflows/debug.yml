name: Debug Cookies V3

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        continue-on-error: true
        with:
          ref: main

      - name: Install dependencies and create install.log
        if: always()
        run: |
          set +e

          echo "=== GITHUB INFO ===" | tee /tmp/install.log
          echo "GITHUB_WORKSPACE=$GITHUB_WORKSPACE" | tee -a /tmp/install.log
          echo "GITHUB_REF=$GITHUB_REF" | tee -a /tmp/install.log
          echo "GITHUB_SHA=$GITHUB_SHA" | tee -a /tmp/install.log
          pwd | tee -a /tmp/install.log
          ls -la "$GITHUB_WORKSPACE" | tee -a /tmp/install.log

          echo "" | tee -a /tmp/install.log
          echo "=== PYTHON / PIP ===" | tee -a /tmp/install.log

          python3 --version 2>&1 | tee -a /tmp/install.log
          echo "PYTHON EXIT CODE: ${PIPESTATUS[0]}" | tee -a /tmp/install.log

          python3 -m pip --version 2>&1 | tee -a /tmp/install.log
          echo "PIP VERSION EXIT CODE: ${PIPESTATUS[0]}" | tee -a /tmp/install.log

          echo "" | tee -a /tmp/install.log
          echo "=== PIP UPGRADE ===" | tee -a /tmp/install.log

          python3 -m pip install --user --upgrade pip 2>&1 | tee -a /tmp/install.log
          echo "PIP UPGRADE EXIT CODE: ${PIPESTATUS[0]}" | tee -a /tmp/install.log

          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          echo "Added $HOME/.local/bin to PATH" | tee -a /tmp/install.log

          echo "" | tee -a /tmp/install.log
          echo "=== INSTALL YT-DLP NIGHTLY + EJS ===" | tee -a /tmp/install.log

          python3 -m pip install --user --upgrade --pre --upgrade-strategy eager "yt-dlp[default] @ https://pypi.org/simple/yt-dlp/" yt-dlp-ejs 2>&1 | tee -a /tmp/install.log
          echo "YT-DLP INSTALL EXIT CODE: ${PIPESTATUS[0]}" | tee -a /tmp/install.log

          echo "" | tee -a /tmp/install.log
          echo "=== INSTALL NODEJS + FFMPEG ===" | tee -a /tmp/install.log

          sudo apt-get update 2>&1 | tee -a /tmp/install.log
          echo "APT UPDATE EXIT CODE: ${PIPESTATUS[0]}" | tee -a /tmp/install.log

          sudo apt-get install -y nodejs ffmpeg 2>&1 | tee -a /tmp/install.log
          echo "APT INSTALL EXIT CODE: ${PIPESTATUS[0]}" | tee -a /tmp/install.log

          echo "" | tee -a /tmp/install.log
          echo "=== VERSIONS ===" | tee -a /tmp/install.log

          export PATH="$HOME/.local/bin:$PATH"
          echo "PATH=$PATH" | tee -a /tmp/install.log

          node --version 2>&1 | tee -a /tmp/install.log
          echo "NODE EXIT CODE: ${PIPESTATUS[0]}" | tee -a /tmp/install.log

          python3 -m yt_dlp --version 2>&1 | tee -a /tmp/install.log
          echo "YT-DLP VERSION EXIT CODE: ${PIPESTATUS[0]}" | tee -a /tmp/install.log

          python3 - <<'PY' | tee -a /tmp/install.log
          import importlib.util

          spec = importlib.util.find_spec("yt_dlp_ejs")
          print("yt_dlp_ejs module:", spec.origin if spec else "NOT FOUND")
          PY

          echo "" | tee -a /tmp/install.log
          echo "=== COOKIE FILES ===" | tee -a /tmp/install.log

          COOKIE_FILES=()

          if [ -f "$GITHUB_WORKSPACE/cookies_new.txt" ]; then
            COOKIE_FILES+=("$GITHUB_WORKSPACE/cookies_new.txt")
          fi

          if [ -f "$GITHUB_WORKSPACE/www.youtube.com_cookies.txt" ]; then
            COOKIE_FILES+=("$GITHUB_WORKSPACE/www.youtube.com_cookies.txt")
          fi

          if [ ${#COOKIE_FILES[@]} -eq 0 ]; then
            echo "ERROR: no cookies file found" | tee -a /tmp/install.log
          else
            for COOKIE_FILE in "${COOKIE_FILES[@]}"; do
              echo "Found cookies file:" | tee -a /tmp/install.log
              echo "$COOKIE_FILE" | tee -a /tmp/install.log
              ls -l "$COOKIE_FILE" | tee -a /tmp/install.log
              wc -c "$COOKIE_FILE" | tee -a /tmp/install.log
            done
          fi

      - name: Debug formats and download attempts
        if: always()
        run: |
          set +e

          URL="https://www.youtube.com/watch?v=I60j6jur_TY"
          CLIENTS=(web ios android tv mweb)
          YTDLP=(python3 -m yt_dlp)

          export PATH="$HOME/.local/bin:$PATH"

          COOKIE_FILES=()

          if [ -f "$GITHUB_WORKSPACE/cookies_new.txt" ]; then
            COOKIE_FILES+=("$GITHUB_WORKSPACE/cookies_new.txt")
          fi

          if [ -f "$GITHUB_WORKSPACE/www.youtube.com_cookies.txt" ]; then
            COOKIE_FILES+=("$GITHUB_WORKSPACE/www.youtube.com_cookies.txt")
          fi

          echo "=== YT-DLP VERSION ===" | tee /tmp/list_formats.txt
          "${YTDLP[@]}" --version 2>&1 | tee -a /tmp/list_formats.txt
          echo "YT-DLP VERSION EXIT CODE: ${PIPESTATUS[0]}" | tee -a /tmp/list_formats.txt

          echo "" | tee -a /tmp/list_formats.txt
          echo "=== LIST FORMATS WITHOUT COOKIES ===" | tee -a /tmp/list_formats.txt

          "${YTDLP[@]}" \
            --force-ipv4 \
            --extractor-args "youtube:player_client=web" \
            --skip-download \
            --list-formats \
            "$URL" \
            2>&1 | tee -a /tmp/list_formats.txt

          echo "FORMAT EXIT CODE (NO_COOKIE, web): ${PIPESTATUS[0]}" | tee -a /tmp/list_formats.txt

          if [ ${#COOKIE_FILES[@]} -eq 0 ]; then
            echo "" | tee -a /tmp/list_formats.txt
            echo "No cookies files found, skipping cookie-based format checks." | tee -a /tmp/list_formats.txt
          else
            for COOKIE_FILE in "${COOKIE_FILES[@]}"; do
              COOKIE_LABEL="$(basename "$COOKIE_FILE")"

              echo "" | tee -a /tmp/list_formats.txt
              echo "=== LIST FORMATS COOKIE=$COOKIE_LABEL ===" | tee -a /tmp/list_formats.txt

              for CLIENT in "${CLIENTS[@]}"; do
                echo "--- CLIENT=$CLIENT ---" | tee -a /tmp/list_formats.txt

                "${YTDLP[@]}" \
                  --cookies "$COOKIE_FILE" \
                  --force-ipv4 \
                  --extractor-args "youtube:player_client=$CLIENT" \
                  --skip-download \
                  --list-formats \
                  "$URL" \
                  2>&1 | tee -a /tmp/list_formats.txt

                echo "FORMAT EXIT CODE ($COOKIE_LABEL, $CLIENT): ${PIPESTATUS[0]}" | tee -a /tmp/list_formats.txt
              done
            done
          fi

          echo "" > /tmp/log.txt
          echo "=== DOWNLOAD ATTEMPTS ===" | tee -a /tmp/log.txt

          SUCCESS="no"

          echo "--- NO_COOKIE CLIENT=web ---" | tee -a /tmp/log.txt

          "${YTDLP[@]}" \
            --force-ipv4 \
            --extractor-args "youtube:player_client=web" \
            --no-playlist \
            -f "ba/b" \
            -o "/tmp/test/test_NO_COOKIE_web.%(ext)s" \
            "$URL" \
            2>&1 | tee -a /tmp/log.txt

          NO_COOKIE_STATUS=${PIPESTATUS[0]}
          echo "DOWNLOAD EXIT CODE (NO_COOKIE, web): $NO_COOKIE_STATUS" | tee -a /tmp/log.txt

          if [ "$NO_COOKIE_STATUS" -eq 0 ]; then
            echo "NO_COOKIE|web" > /tmp/success.txt
            SUCCESS="yes"
          fi

          if [ "$SUCCESS" != "yes" ] && [ ${#COOKIE_FILES[@]} -gt 0 ]; then
            for COOKIE_FILE in "${COOKIE_FILES[@]}"; do
              COOKIE_LABEL="$(basename "$COOKIE_FILE")"

              for CLIENT in "${CLIENTS[@]}"; do
                echo "--- COOKIE=$COOKIE_LABEL CLIENT=$CLIENT ---" | tee -a /tmp/log.txt

                "${YTDLP[@]}" \
                  --cookies "$COOKIE_FILE" \
                  --force-ipv4 \
                  --extractor-args "youtube:player_client=$CLIENT" \
                  --no-playlist \
                  -f "ba/b" \
                  -o "/tmp/test/test_${COOKIE_LABEL}_${CLIENT}.%(ext)s" \
                  "$URL" \
                  2>&1 | tee -a /tmp/log.txt

                STATUS=${PIPESTATUS[0]}
                echo "DOWNLOAD EXIT CODE ($COOKIE_LABEL, $CLIENT): $STATUS" | tee -a /tmp/log.txt

                if [ "$STATUS" -eq 0 ]; then
                  echo "$COOKIE_LABEL|$CLIENT" > /tmp/success.txt
                  SUCCESS="yes"
                  break 2
                fi
              done
            done
          fi

          if [ "$SUCCESS" != "yes" ]; then
            echo "" | tee -a /tmp/log.txt
            echo "All download attempts failed." | tee -a /tmp/log.txt
          else
            echo "" | tee -a /tmp/log.txt
            echo "SUCCESS:" | tee -a /tmp/log.txt
            cat /tmp/success.txt | tee -a /tmp/log.txt
          fi

      - name: Send email
        if: always()
        env:
          EMAIL_FROM: p0556722432@gmail.com
          EMAIL_TO: p0556722432@gmail.com
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: |
          python3 << 'EOF'
          import smtplib
          from email.mime.text import MIMEText
          from email.mime.multipart import MIMEMultipart
          import os

          paths = [
              "/tmp/install.log",
              "/tmp/list_formats.txt",
              "/tmp/log.txt",
              "/tmp/success.txt"
          ]

          log_content = ""

          for path in paths:
              if os.path.exists(path):
                  with open(path, "r", errors="replace") as f:
                      log_content += f"\n\n========== {path} ==========\n"
                      log_content += f.read()
              else:
                  log_content += f"\n\n========== {path} MISSING ==========\n"

          msg = MIMEMultipart()
          msg["From"] = os.environ["EMAIL_FROM"]
          msg["To"] = os.environ["EMAIL_TO"]
          msg["Subject"] = "YouTube Debug Log V3"

          msg.attach(MIMEText(log_content, "plain"))

          try:
              server = smtplib.SMTP("smtp.gmail.com", 587)
              server.starttls()
              server.login(os.environ["EMAIL_FROM"], os.environ["EMAIL_PASSWORD"])
              server.sendmail(
                  os.environ["EMAIL_FROM"],
                  os.environ["EMAIL_TO"],
                  msg.as_string()
              )
              server.quit()
              print("EMAIL SENT")
          except Exception as e:
              print(f"EMAIL FAILED: {e}")
          EOF
