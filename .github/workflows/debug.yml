name: Debug Simple

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Install and test
        run: |
          echo "AAA - Starting"
          pip3 install yt-dlp 2>&1
          echo "BBB - Install done"
          yt-dlp --version 2>&1
          echo "CCC - Version shown"
