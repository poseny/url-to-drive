name: Upload URL → Google Drive (real ZIP, any size)

# מאפשר ריצה ידנית עם שלושה inputs
on:
  workflow_dispatch:
    inputs:
      url:
        description: "כתובת URL של הקובץ להורדה"
        required: true
        default: "https://upload.wikimedia.org/wikipedia/commons/d/d6/Her_First_Kiss_%281919%29.webm"
      folder:
        description: "תיקיית יעד ב‑Drive (ללא שם קובץ)"
        required: true
        default: "MyUploads"
      filename:
        description: "שם קובץ לפני .zip (אם ריק – נלקח מה‑URL)"
        required: false
        default: ""

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      # --------------------------------------------------------------
      # 1️⃣ התקנת rclone (zip כבר קיים במערכת Ubuntu)
      # --------------------------------------------------------------
      - name: Install rclone
        run: |
          curl https://rclone.org/install.sh | sudo bash

      # --------------------------------------------------------------
      # 2️⃣ כתיבת קונפיג של rclone (remote בשם gdrive)
      # --------------------------------------------------------------
      - name: Write rclone config
        env:
          RCLONE_GDRIVE_TOKEN: ${{ secrets.RCLONE_GDRIVE_TOKEN }}
        run: |
          mkdir -p ~/.config/rclone
          cat > ~/.config/rclone/rclone.conf <<EOF
          [gdrive]
          type = drive
          scope = drive.file
          token = $RCLONE_GDRIVE_TOKEN
          EOF

      # --------------------------------------------------------------
      # 3️⃣ חישוב שמות – zip_name (ב‑Drive) + internal_name (ב‑ZIP)
      # --------------------------------------------------------------
      - name: Determine names
        id: names
        env:
          FILE_URL: ${{ github.event.inputs.url }}
          USER_NAME: ${{ github.event.inputs.filename }}
          FOLDER: ${{ github.event.inputs.folder }}
        run: |
          set -e

          # 3.1 – שם הקובץ כפי שמופיע ב‑URL (לדוגמה 480.mp4)
          raw_name=$(basename "$FILE_URL")
          raw_name=${raw_name%%\?*}                          # מוריד query‑string אם קיים
          decoded_name=$(printf '%b' "$(echo -e "${raw_name//%/\\x}")")
          echo "🔤 Decoded name from URL = $decoded_name"

          # 3.2 – שם בסיס לקובץ ה‑ZIP (אם ה‑input filename ריק → משתמש בשם ללא סיומת)
          if [[ -n "$USER_NAME" ]]; then
            base_name="$USER_NAME"
          else
            base_name="${decoded_name%.*}"                  # מוחק סיומת קיימת
          fi

          zip_name="${base_name}.zip"                       # שם קובץ ה‑ZIP בתיקיית Drive
          internal_name="$decoded_name"                     # שם הקובץ הפנימי ב‑ZIP

          echo "📦 ZIP name on Drive      = $zip_name"
          echo "📁 Internal name in ZIP   = $internal_name"

          remote_path="gdrive:${FOLDER}/${zip_name}"
          echo "zip_name=$zip_name"        >> $GITHUB_OUTPUT
          echo "internal_name=$internal_name" >> $GITHUB_OUTPUT
          echo "remote_path=$remote_path"  >> $GITHUB_OUTPUT

      # --------------------------------------------------------------
      # 4️⃣ מחיקת קובץ קיים (אם קיים) – כך שלא נשארים גרסאות ישנות
      # --------------------------------------------------------------
      - name: Delete previous ZIP (if any)
        env:
          REMOTE_PATH: ${{ steps.names.outputs.remote_path }}
        run: |
          if rclone lsf "$REMOTE_PATH" > /dev/null 2>&1; then
            echo "🗑️ Deleting old $REMOTE_PATH"
            rclone delete "$REMOTE_PATH"
          else
            echo "✅ No previous file found"
          fi

      # --------------------------------------------------------------
      # 5️⃣ ההמרה האמיתית – download → zip (with internal name) → upload
      # --------------------------------------------------------------
      - name: Download → zip (preserve internal name) → upload
        env:
          FILE_URL: ${{ github.event.inputs.url }}
          INTERNAL_NAME: ${{ steps.names.outputs.internal_name }}
          DEST_PATH: ${{ steps.names.outputs.remote_path }}
        run: |
          set -e
          set -o pipefail               # אם שלב במעבר נכשל – נכשל ה‑job

          echo "🚀 Starting transfer"
          echo "URL            = $FILE_URL"
          echo "Internal name  = $INTERNAL_NAME"
          echo "Destination    = $DEST_PATH"

          # ----------------‑הפעלה--------------------
          #   wget   → קורא את הקובץ מה‑URL
          #   zip    → יוצר ZIP ב‑stdout,
          #            -:"$INTERNAL_NAME" מציין שם הפנימי,
          #            - (ה‑argument האחרון) הוא קובץ קלט מ‑stdin
          #   rclone → מעביר את ה‑ZIP הישר ל‑Google Drive.
          # -------------------------------------------------
          wget -q -O - "$FILE_URL" \
            | zip -q - -:"$INTERNAL_NAME" - \
            | rclone rcat "$DEST_PATH" --progress
